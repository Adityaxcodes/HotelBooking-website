{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "validate-env": "node -e \"console.log('Validating server environment...'); const requiredVars = ['CLERK_PUBLISHABLE_KEY', 'CLERK_SECRET_KEY', 'DBURI']; const missing = requiredVars.filter(v => !process.env[v]); if (missing.length) { console.error('❌ Missing environment variables:', missing.join(', ')); process.exit(1); } const key = process.env.CLERK_PUBLISHABLE_KEY; const isProduction = key.startsWith('pk_live_'); console.log('✅ Environment:', isProduction ? 'Production' : 'Development'); console.log('✅ Database configured'); console.log('✅ Clerk configured'); console.log('Server environment validation complete!');\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@clerk/clerk-sdk-node": "^5.1.6",
    "@clerk/express": "^1.7.13",
    "cloudinary": "^2.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.5",
    "multer": "^2.0.2",
    "nodemailer": "^6.9.18",
    "svix": "^1.69.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": ">=18.x <23.x",
    "npm": ">=9.0.0"
  }
}
